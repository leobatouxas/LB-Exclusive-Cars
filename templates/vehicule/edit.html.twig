{% extends 'base.html.twig' %}

{% block title %}Edit Vehicule
{% endblock %}

{% block body %}
	<div class="container mx-auto my-2">
		<div class="flex flex-wrap justify-center -mx-3 mb-3">
			<div>
				<h1 class="text-4xl font-medium mx-2">Modification</h1>
			</div>
			<div>
				{{ include('vehicule/_delete_form.html.twig') }}
            </div>
		</div>
		{{ include('vehicule/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	</div>

{% endblock %}
